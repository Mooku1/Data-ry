<h1>All Users</h1>
	
	<ul>
		<% @users.each do |u| %>
		<li>
			<%= u.username %>
			<%= link_to "View User", user_path(u) %>
		</li>
		<% end %>
	</ul>
	
	<%= link_to "Add new User", new_user_path %>